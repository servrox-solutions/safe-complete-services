---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-webhooks-worker
  labels:
    service: transaction-webhooks-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-webhooks-worker
  template:
    metadata:
      labels:
        app: transaction-webhooks-worker
    spec:
      initContainers:
        - {{ include "safe.transaction_service.create_db_container" . | nindent 10 }}
      containers:
        - name: django
          image: {{ .Values.transaction_service.image.name }}:{{ .Values.transaction_service.image.tag }}
          command: [bash]
          args:
            - -c
            - |
              {{ .Files.Get "files/transaction-service/web/celery/worker/run.sh" | nindent 14 }}
          env:
            {{ include "safe.transaction_service.common_env" . | nindent 12 }}
            - name: RUN_MIGRATIONS
              value: "1"
            - name: WORKER_QUEUES
              value: "notifications,webhooks"
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
            limits:
              cpu: 300m
              memory: 300Mi
